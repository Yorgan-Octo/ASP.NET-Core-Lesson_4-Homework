<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ваша сторінка</title>
    <link rel="stylesheet" href="styles.css"> <!-- Додайте посилання на ваш CSS-файл -->
    <style>
        /* Стилі для шапки */
        header {
            background-color: #333;
            color: #fff;
            padding: 10px 0;
            text-align: center;
            position: relative;
            top: 0;
            left: 0;
            right: 0;
            margin-top: -12px;
            margin-left: -15px;
            margin-right: -15px;
        }

        header h1 {
            margin: 0;
        }

        nav ul {
            list-style-type: none;
            padding: 0;
        }

        nav ul li {
            display: inline;
            margin-right: 20px;
        }

        nav ul li a {
            color: #fff;
            text-decoration: none;
        }

        /* Стилі для центральної частини */
        main {
            padding: 20px 20px; /* Відступ зверху і знизу для шапки і підвала */
        }

        .post {
            background-color: #f9f9f9;
            border: 1px solid #ddd;
            margin-bottom: 20px;
            padding: 10px;
        }

        .post h2 {
            margin-top: 0;
        }

        /* Стилі для підвала */
        footer {
            background-color: #333;
            color: #fff;
            padding: 10px 0;
            text-align: center;
            position: relative;
            bottom: 0;
            left: 0;
            right: 0;
            flex-shrink: 0;
            margin-bottom: -12px;
            margin-left: -15px;
            margin-right: -15px;
        }
    </style>
</head>
<body>
    <!-- Шапка -->
    <header>
        <h1 style="margin-top: 5px;">Sakura Blog</h1>
    </header>
        
    @RenderBody()

    <!-- Підвал -->
    <footer>
        <p>&copy; 2023 Ваша компанія. Усі права захищені.</p>
    </footer>
</body>
</html>
